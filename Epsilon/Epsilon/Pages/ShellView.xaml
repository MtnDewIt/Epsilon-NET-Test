<controls:ChromeWindow
    x:Class="Epsilon.Pages.ShellView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:EpsilonLib.Controls;assembly=EpsilonLib"
    xmlns:EpsilonConverters="clr-namespace:EpsilonLib.Converters;assembly=EpsilonLib"
    xmlns:local="clr-namespace:WpfApp20"
    xmlns:s="https://github.com/canton7/Stylet"
    xmlns:xcad="http://schemas.xceed.com/wpf/xaml/avalondock"
    Title="{Binding Title}"
    Topmost="{DynamicResource AlwaysOnTop}"
    Width="1280"
    Height="800"
    MinWidth="500"
    MinHeight="281">
    <DockPanel AllowDrop="true" Drop="Shell_Drop">
        <Grid
            HorizontalAlignment="Stretch"
            DockPanel.Dock="Top">
            <StackPanel
                Margin="0,3,0,0"
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                Orientation="Horizontal">
                <Image
                    Style="{StaticResource EpsilonIcon}"
                    Margin="4,1,4,0"/>
                <Menu
                    FontSize="15"
                    ItemContainerStyle="{StaticResource MenuItemStyle}"
                    ItemsSource="{Binding MainMenu}"
                    WindowChrome.IsHitTestVisibleInChrome="True" />
            </StackPanel>
            <TextBlock
                Style="{StaticResource EpsilonTitle}"
                Margin="0,4,0,0"
                FontSize="18"
                Text="{Binding Title}" />
        </Grid>


        <StatusBar
            Height="Auto"
            Background="{DynamicResource Accent}"
            BorderThickness="0"
            DockPanel.Dock="Bottom">
            <StatusBarItem>
                <TextBlock
                    Margin="3,0,0,0"
                    FontSize="14"
                    Foreground="{DynamicResource Title}"
                    Text="{Binding StatusBar.StatusText}" />
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <ProgressBar
                    Width="80"
                    Height="14"
                    IsIndeterminate="{Binding StatusBar.ProgressIndeterminate}"
                    Maximum="100"
                    Minimum="0"
                    Visibility="{Binding StatusBar.ProgressVisible, Converter={EpsilonConverters:BoolToVisibilityConverter}}"
                    Value="{Binding StatusBar.ProgressPercent}" />
            </StatusBarItem>
        </StatusBar>
        <xcad:DockingManager
            Margin="6,6,4,4"
            ActiveContent="{Binding ActiveDocument, Mode=TwoWay}"
            AllowMixedOrientation="True"
            DocumentsSource="{Binding Items}">
            <xcad:DockingManager.DocumentContextMenu>
                <ContextMenu>
                    <MenuItem
                        Command="{Binding Model.CopyFullPath}"
                        Header="Copy Full Path" />
                    <MenuItem
                        Command="{Binding Model.OpenContainingFolder}"
                        Header="Open Containing Folder" />
                    <Separator />
                    <MenuItem
                        Command="{Binding Path=CloseCommand}"
                        Header="{x:Static xcad:Resources.Document_Close}"
                        Visibility="{Binding Path=IsEnabled, RelativeSource={RelativeSource Self}, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
                    <MenuItem
                        Command="{Binding Path=CloseAllButThisCommand}"
                        Header="{x:Static xcad:Resources.Document_CloseAllButThis}" />
                    <MenuItem
                        Command="{Binding Path=CloseAllCommand}"
                        Header="{x:Static xcad:Resources.Document_CloseAll}" />
                    <MenuItem
                        Command="{Binding Path=FloatCommand}"
                        Header="{x:Static xcad:Resources.Document_Float}" />
                    <MenuItem
                        Command="{Binding Path=DockAsDocumentCommand}"
                        Header="{x:Static xcad:Resources.Document_DockAsDocument}" />
                    <MenuItem
                        Command="{Binding Path=NewHorizontalTabGroupCommand}"
                        Header="{x:Static xcad:Resources.Document_NewHorizontalTabGroup}"
                        Visibility="{Binding Path=IsEnabled, RelativeSource={RelativeSource Self}, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                        <MenuItem.Icon>
                            <Image
                                Width="14"
                                Source="../Themes/Controls/AvalonDock/Images/HTabGroup.png"
                                Stretch="Uniform" />
                        </MenuItem.Icon>
                    </MenuItem>

                    <MenuItem
                        Command="{Binding Path=NewVerticalTabGroupCommand}"
                        Header="{x:Static xcad:Resources.Document_NewVerticalTabGroup}"
                        Visibility="{Binding Path=IsEnabled, RelativeSource={RelativeSource Self}, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                        <MenuItem.Icon>
                            <Image
                                Width="14"
                                Source="../Themes/Controls/AvalonDock/Images/VTabGroup.png"
                                Stretch="Uniform" />
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem
                        Command="{Binding Path=MoveToNextTabGroupCommand}"
                        Header="{x:Static xcad:Resources.Document_MoveToNextTabGroup}"
                        Visibility="{Binding Path=IsEnabled, RelativeSource={RelativeSource Self}, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
                    <MenuItem
                        Command="{Binding Path=MoveToPreviousTabGroupCommand}"
                        Header="{x:Static xcad:Resources.Document_MoveToPreviousTabGroup}"
                        Visibility="{Binding Path=IsEnabled, RelativeSource={RelativeSource Self}, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
                </ContextMenu>
            </xcad:DockingManager.DocumentContextMenu>
            <xcad:LayoutRoot>
                <xcad:LayoutPanel
                    x:Name="VerticalPanel"
                    Orientation="Vertical">
                    <xcad:LayoutPanel
                        x:Name="HorizontalPanel"
                        Orientation="Horizontal">
                        <xcad:LayoutDocumentPane />
                    </xcad:LayoutPanel>
                </xcad:LayoutPanel>
            </xcad:LayoutRoot>
            <xcad:DockingManager.LayoutItemContainerStyleSelector>
                <local:PanesStyleSelector>
                    <local:PanesStyleSelector.DocumentStyle>
                        <Style TargetType="{x:Type xcad:LayoutItem}">
                            <Setter Property="ContentId" Value="{Binding Model.ContentId}" />
                            <Setter Property="Title" Value="{Binding Model.DisplayName, Mode=OneWay}" />
                            <Setter Property="CloseCommand" Value="{Binding Model.CloseCommand}" />
                            <Setter Property="IsSelected" Value="{Binding Model.IsSelected, Mode=TwoWay}" />
                            <Setter Property="ToolTip" Value="{Binding Model.CacheFile.File.FullName, Mode=OneWay}" />
                        </Style>
                    </local:PanesStyleSelector.DocumentStyle>
                    <local:PanesStyleSelector.ToolStyle>
                        <Style TargetType="{x:Type xcad:LayoutAnchorableItem}">
                            <Setter Property="ContentId" Value="{Binding Model.ContentId}" />
                            <Setter Property="Title" Value="{Binding Model.DisplayName, Mode=OneWay}" />
                            <Setter Property="CloseCommand" Value="{Binding Model.CloseCommand}" />
                            <Setter Property="IsSelected" Value="{Binding Model.IsSelected, Mode=TwoWay}" />
                            <Setter Property="ToolTip" Value="{Binding Model.CacheFile.File.FullName, Mode=OneWay}" />
                        </Style>
                    </local:PanesStyleSelector.ToolStyle>
                </local:PanesStyleSelector>
            </xcad:DockingManager.LayoutItemContainerStyleSelector>
            <xcad:DockingManager.LayoutItemTemplate>
                <DataTemplate>
                    <ContentControl s:View.Model="{Binding}" />
                </DataTemplate>
            </xcad:DockingManager.LayoutItemTemplate>
        </xcad:DockingManager>
    </DockPanel>

</controls:ChromeWindow>
