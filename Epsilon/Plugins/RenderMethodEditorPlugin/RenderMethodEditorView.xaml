<UserControl x:Class="RenderMethodEditorPlugin.RenderMethodEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RenderMethodEditorPlugin"
             xmlns:s="https://github.com/canton7/Stylet"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type local:BooleanConstant}">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding Name}" Width="200"/>
                <CheckBox IsChecked="{Binding Value, Mode=TwoWay}" ToolTip="{Binding Description}" Width="100" HorizontalAlignment="Right"/>
            </StackPanel>
        </DataTemplate>
    </UserControl.Resources>
    
    <Grid>
        <StackPanel Orientation="Vertical" Margin="4">
            <TextBlock Text="Boolean Constants" />
            <Border BorderThickness="1">
                <ListBox ItemsSource="{Binding BooleanConstants}" />
            </Border>
        </StackPanel>
        <!-- <Button Click="{s:Action Test}" Content="Sync" /> -->
    </Grid>
</UserControl>
